{
  "name": "english-guide",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "homepage": "https://endorrfin.github.io/english-guide/",
  "license": "MIT",
  "author": "Vasyl Krupka",
  "description": "A deep, interactive, bilingual (EN/UA) guide to <DOMAIN>.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "lint": "eslint .",
    "gen:index": "tsx scripts/gen-data-index.ts",
    "predev": "npm run gen:index",
    "prebuild": "npm run gen:index",
    "check:index": "tsx scripts/check-data-index.ts",
    "check:bundle": "tsx scripts/check-bundle.ts",
    "check:data": "tsx scripts/check-data.ts",
    "test": "tsx scripts/run-tests.ts",
    "smoke": "tsx --tsconfig tsconfig.app.json scripts/smoke.ts",
    "verify": "npm run typecheck && npm run lint && npm run check:index && npm run check:data && npm run test && npm run smoke && npm run build && npm run check:bundle"
  },
  "//test+smoke": "standard §4.6. `test` auto-discovers scripts/test-*.ts (run-tests.ts) — green when none exist yet; add a pure engine in src/lib + scripts/test-<name>.ts and it's picked up. `smoke` SSR-renders every sim/figure/page (scripts/smoke.ts) — wire its 4 TODO spots after the golden module. Both run only meaningfully once the golden module exists; run `verify` from then on.",
  "//meta-split": "DONE (M1, standard §4.4): scripts/gen-data-index.ts emits the slim src/data/words/index.generated.ts (WORD_INDEX + WORD_COUNTS) and src/data/reading/index.generated.ts (READING_COUNTS); predev/prebuild regenerate, check:index fails on staleness. The eager shell imports ONLY those; the full corpora live in lazy chunks (vite manualChunks: words/reading/idioms). Adding a field to WORD_INDEX is cheap — adding a BIG one is not, measure first. The module meta-split (data/meta.ts) is still open.",
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.0",
    "@types/node": "^22.20.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^6.0.0",
    "eslint": "^10.0.0",
    "eslint-plugin-react-hooks": "^7.0.0",
    "eslint-plugin-react-refresh": "^0.5.0",
    "globals": "^17.0.0",
    "playwright": "^1.56.0",
    "tsx": "^4.22.0",
    "typescript": "^6.0.0",
    "typescript-eslint": "^8.62.0",
    "vite": "^8.1.0"
  }
}
