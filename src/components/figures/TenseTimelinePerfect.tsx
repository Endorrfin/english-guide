// CHANGED (T3): the Perfect zone of the parametric TenseTimeline figure — the flagship contrast of
// m10: ● Past Simple (a finished dot, cut off in the past) vs ⤺ Present Perfect (the same past
// action, but linked forward to NOW). Registry key 'timeline-perfect' (m10). One export per file
// keeps smoke's files===keys invariant; the parametric core lives in ../tense/TenseTimeline.
import { TenseTimeline } from '../tense/TenseTimeline';

export function TenseTimelinePerfect() {
  return <TenseTimeline zone="perfect" />;
}
